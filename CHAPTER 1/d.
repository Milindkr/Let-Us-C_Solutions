/*Temperature of a city in Fahrenheit degrees is input through the 
keyboard. Write a program to convert this temperature into 
Centigrade degrees.
*/


#include <stdio.h>

int main()
{
    float temp;
    printf("Enter Temperature of your city in Fahrenheit : ");
    scanf("%f",&temp);

    float temp_in_centigrade;

    temp_in_centigrade = (temp-32)*0.555;

    printf("Temperature in Centigrade is = %f",temp_in_centigrade);

    return 0;
}
